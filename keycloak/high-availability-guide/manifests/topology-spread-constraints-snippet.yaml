# Sample snippet for workloads that need AZ spreading.
# Adapt labels and scheduling policy to your chart and cluster standards.

topologySpreadConstraints:
  - maxSkew: 1
    topologyKey: topology.kubernetes.io/zone
    whenUnsatisfiable: ScheduleAnyway
    labelSelector:
      matchLabels:
        app.kubernetes.io/name: keycloak
